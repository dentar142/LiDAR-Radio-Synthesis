"""Radio-map calibration and query-matched expert routing.

The public command-line adapters live outside :mod:`src.radio.legacy`.  The
legacy package preserves the experiment implementation and deliberately does
not import optional scientific dependencies at package import time.
"""

__all__: tuple[str, ...] = ()
